# ndd20

> **세션 시작 시 `HANDOFF.md` 를 읽어 현재 상황을 파악할 것.**
> 앞으로 할 일은 `TODOs.md`, 지나간 것은 `devlog/` 에 있다.

공개 자료 [NDD20](https://doi.org/10.25405/data.ncl.c.4982342)(Northumberland
Dolphin Dataset 2020) 위에서, 형제 저장소 [`dolfinserver2`](../dolfinserver2)
가 세운 **방법이 건너가는지**를 재는 자리.

## 왜 따로 있나

**간섭을 피하려는 것이다.** 저쪽은 사람의 판정이 든 `fin.db` 와 운영 화면 둘을
들고 도는 자리이고, 여기서 하는 일은 **남의 자료로 재는 것**이다. 한 저장소에
두면 격자·설정·마이그레이션이 섞이고, 섞이면 **어느 쪽 숫자인지 갈리지 않는다.**

- 저쪽 코드를 **고치지 않는다.** 가져올 것이 있으면 **베껴 오고 어디서 왔는지
  적는다** — 얽어 두면 이 저장소 때문에 운영이 흔들린다
- 저쪽 가중치(`reid/blocks/`)는 **읽기만** 한다
- `fin.db` 는 **안 연다**

## Django 는 **보는 화면에만** 쓴다

저쪽이 Django 인 것은 **사람의 판정을 담는 DB** 와 **검토 화면** 때문이다.
여기는 라벨이 이미 자료에 들어 있어 **DB 쪽 까닭이 없다.** 그래서 명령은 Django
밖에 둔다 — 평범한 파이썬 꾸러미에 `main.py` 하나가 명령을 고르는 꼴이고,
**명령 하나가 한 가지 일을 하고 도움말이 곧 문서**인 성질은 그대로 가져온다.

**화면 쪽 까닭은 남았다.** 2026-09-07 에 *"정적 HTML 한 장이면 된다"* 를 걷고
`manage.py`·`nddweb/`·`viewer/` 를 세웠다(다음 절). 다만 **DB 도 로그인도 세션도
안 켠다** — `DATABASES = {}` 다. 담을 판정이 없는데 켜 두면 없어도 될
`db.sqlite3` 가 생기고, 그것이 있으면 언젠가 무언가가 그리로 쓴다.

## 보는 화면은 **받을 것이 있나 없나**로 가른다

저쪽에서 배운 것은 *"검토 화면을 일찍 만든 것이 가장 값이 컸다"* 인데, 그 화면이
값을 한 까닭은 **사람의 판정을 받아** 다음 학습으로 돌려보냈기 때문이다.
여기는 그 고리가 처음부터 있는 자리와 없는 자리가 갈린다.

| 하는 일 | 정답이 | 사람에게 받을 것 | 그래서 |
|---|---|---|---|
| 분할 채점 (`ABOVE` 폴리라인 2,939) | **이미 있다** | 없다 — 보는 것은 **진단**이다 | **보기만 하는 화면** |
| re-ID 채점 (개체 ID) | **이미 있다** | 없다 | 보기만 하는 화면 |
| 남의 라벨이 틀렸나 · 실패의 까닭 대기 | 없다 | **있다** | 아래 |

**앞의 둘에는 받을 것이 없다.** 정답이 자료에 들어 있으니 화면이 하는 일은
*"왜 틀렸나" 를 눈으로 아는 것*이고, 그것은 코드를 고치게 만들지 자료를
고치게 만들지 않는다.

### 걷은 것 — **정적 HTML 한 장** (2026-09-07)

*받을 것이 없다* 에서 *서버가 필요 없다* 로 건너간 것이 틀렸다. 둘은 다른 말이다.

- 정적이어도 **사진 2,201장(장당 5184×3456)을 어떻게든 내려보내야 한다**
- NAS 원본을 `file://` 로 여는 것은 못 쓸 만큼 느리다
- 볼 단위가 사진이 아니라 **지느러미**라 잘라 내야 하고, 잘라 낸 것을 어딘가
  쌓아야 한다 (`out/thumbs/` · 480px · 처음 0.27초 · 그 뒤 1.5ms)

**서버가 오히려 간단했다.** 가르는 선은 서버냐 아니냐가 아니라 **받느냐 안
받느냐**이고, 그 선은 그대로다 — 이 화면은 아무것도 안 받는다.

한 가지는 정말 잃었다. 정적 쪽 논거였던 *"뽑아 둔 장이 그 판의 기록으로 남는다"*
가 살아 있는 화면에는 없다. 그러니 **기록은 화면이 아니라 명령이 파일로 떨군
것**이다 — 성적을 화면에서 읽고 옮겨 적지 않는다.

**받을 것이 생기면 그때 더 세운다.** 순서는 이렇게 둔다:

1. **보기만 하는 화면** — 지금 있는 것 (`python manage.py runserver`)
2. **표시 내보내기** — 페이지가 `localStorage` 에 표시를 담고 단추 하나로
   JSON 을 떨군다. 명령이 그것을 도로 읽는다. **DB 가 없는데도 기록이 남는다** —
   판정이 몇십·몇백일 때 여기까지가 값이 맞다
3. **DB 와 대기열** — 정렬·여럿이 쓰기가 필요해지면. 그때는 새로 짓지 말고
   **형제 저장소의 `review` 앱을 본떠 온다**

**2를 미리 짓지 않는다** — 받을 판정이 있는지부터 알고 짓는다.

## 이 자료에 대해 먼저 알 것

`python main.py look` 이 세어 준다. 2026-09-07 에 센 것:

| | 사진 | 영역 | 개체 ID | 개체 | 개체당(중앙값) |
|---|---:|---:|---:|---:|---:|
| **ABOVE** (수면 위 · 등지느러미) | 2,201 | 2,939 | **424 (14%)** | 44 | 9 |
| **BELOW** (수중 · 몸통) | 2,201 | 3,163 | 2,233 (71%) | 82 | 21 |

- **ABOVE 가 우리 문제와 같은 것**인데 개체 ID 가 얇다(424). 종은 두껍다
  (BND 2,668 · WBD 271)
- **BELOW 는 다른 문제다** — 뒷날 notch 가 아니라 몸통 무늬. 대신 라벨이 두껍다
- **폴리라인이지 마스크가 아니다** — 채우는 규칙을 우리가 정해야 한다

## 가장 큰 함정 — **날짜가 없다**

`file_attributes` 가 비어 있고, 파일명이 일련번호이며, **EXIF 도 지워져 있다.**

형제 저장소가 성적에 붙이는 첫 규칙이 **관찰일로 가르기**다. 같은 날 사진은 몇
초 사이의 거의 같은 그림이라, 그중 몇 장이 배움에 몇 장이 잼에 들어가면 모델은
개체가 아니라 **그 장면을 외운 것**이 된다 (실측: 같은 날 짝 AUC 0.698 대 날
건너뜀 0.480).

**여기서는 그 자를 못 쓴다.** 그래서:

- **세션을 짐작해 만들지 않는다.** 파일 번호 이웃이나 그림 유사도로 묶는 것은
  엄밀해 *보이는* 것을 만들 뿐이고, 틀렸을 때 그 사실이 안 보인다
- 무작위로 가르되 **그 수를 "상한" 이라고 못박는다.** 문서에만 적지 않고
  **명령이 찍게** 한다 — 규칙을 글로만 두면 안 지켜진다는 것을 저쪽에서 두 번
  배웠다
- **부푼 정도를 잰다.** 맞힌 질의마다 정답을 준 갤러리 조각이 얼마나 닮았는지를
  함께 낸다. 코사인이 아주 높으면 거의 같은 그림(연사)일 공산이 크다 —
  날짜 없이, 짐작 없이, 있는 것만으로 잴 수 있는 자다

## 구조

```
main.py            명령을 고른다 (Django 를 안 거친다)
ndd/labels.py      **NDD20 라벨을 읽는 자리 하나.** 무엇이 "개체 ID 가 있다" 인지도 여기
ndd/crop.py        **자르는 틀을 정하는 자리 하나.** 화면과 명령이 같은 틀을 쓴다
ndd/results.py     **`fins` 가 떨군 판을 읽는 자리 하나.** 적는 쪽과 갈리면
                   칸 이름이 조용히 어긋난다
ndd/commands/      명령 하나가 한 가지 일. 도움말이 곧 문서다
  look             표본을 먼저 눈으로 본다 (세는 자리 하나)
  fins             남의 사진에서 지느러미를 세운다. **판을 JSONL 로 떨군다**

requirements.txt   django · pillow. **제 venv 를 쓴다**(`~/venv/ndd20`) —
                   형제 저장소의 것을 빌리면 저쪽 판올림에 딸려 간다
requirements-fins.txt  torch · ultralytics · opencv. **`fins` 만** 든다 —
                   GPU 없는 기계가 보고 세는 데는 필요가 없다
manage.py          보는 화면만 Django 다
nddweb/            설정·주소. **DB 도 로그인도 세션도 안 켠다**
viewer/            격자(`/`) · 개체별(`/ind/<개체>`) · 잘라 낸 그림(`/img/...`)
                   윤곽은 서버가 안 굽는다 — 0~1 좌표를 브라우저가 SVG 로 얹는다
                   **한 칸에 셋이 겹친다**: 남의 윤곽 · 우리 마스크 · 밑동 현.
                   **화면은 다시 재지 않는다** — 판에 적힌 것을 얹기만 한다

docs/              자료의 생김새. **세어서 확인한 것만** 적는다
out/               미리보기 캐시와 돌린 판. 저장소에 안 담는다 — **기계 사이로
                   건너갈 것은 NAS 에 둔다**(`<NAS>/DolFinID/ndd20/`)
devlog/            지나간 것
```

## 규약

형제 저장소와 같다.

- 커밋 메시지: 영문 첫 줄 요약 + 한국어 본문 (**왜 그렇게 했고 무엇을 버렸는지**)
- `devlog/YYYYMMDD_nnn_제목.md`
- 남에게서 가져온 숫자에는 **어디서 왔는지**를 붙인다
- **성적을 내기 전에 "이 자가 무엇을 재나" 를 묻는다** — 저쪽이 하루에 세 번
  걸린 자리다

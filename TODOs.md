# TODOs

앞으로 할 일. 끝난 것은 며칠만 두고 지운다 — 기록은 `devlog/` 에 남는다.

## 다음

- [ ] **조각 고르는 문턱을 코드에 넣는다 — 먼저 할 것.** `fins` 가 떨군 판을
      읽어 깔때기를 내는 명령이 있어야 한다(담김 · 넓이비 · 틈 · 밑동).
      지금은 그 수를 손으로 내고 있어 **되짚어지지 않는다** — 앞 판에 적혀
      있던 224 · 34개체 · 217조각을 판에서 되짚었더니 280 · 36 · 275 였다
      (`devlog/…_005`). 문턱이 코드에 없으면 다음 판에서 수가 달라졌을 때
      **코드가 바뀐 탓인지 문턱을 달리 잡은 탓인지 못 가린다.**
      **넓이비는 판에 없다** — 남의 폴리곤 넓이를 라벨에서 다시 재야 나온다
- [ ] **담김 낮은 247개를 갈른다** — 눈으로만 갈린다. 화면이 이미 나쁜 것부터
      세워 준다(`?only=low&sort=hold`). 두 무더기다:
      **(가)** 초록 윤곽이 지느러미를 빼고 몸통만 두른 것 — **남의 라벨 규약**
      **(나)** 몸통·물보라를 통째로 잡은 것 — **우리 실패**
      가른 뒤라야 *"98%에 마스크를 냈다"* 가 무슨 뜻인지 말할 수 있다.
      **세는 자리가 없으니 표시를 어디 담을지부터 정할 것**
      (`CLAUDE.md` 의 2단계 — 여기가 그것이 필요해지는 첫 자리다)
- [ ] **밑동을 손본다 — 가장 약하다.** 마스크 2,881 중 2,186(76%)만 가고,
      개체 ID 붙은 424 에서는 292(69%)다. **못 찾는 것만이 아니다** — 찾은 것
      중에도 몸통을 가로지른 현이 있다. `pose-v1` 이 배운 밑동은 **수면선**인데
      NDD20 은 물결도 각도도 다르다. 왜 못 찾는지부터 얹어서 볼 것
- [ ] **re-ID 를 잰다 — 36개체 · 조각 275.** 저쪽 분류기는 **못 쓴다**(저쪽
      개체에 맞춰 녹인 것이고 NDD20 개체는 거기 없다). **임베딩만 꺼내 갤러리
      최근접**으로 잰다. 날짜가 없으니 무작위로 가르고 **그 수를 상한이라고
      명령이 찍는다.** 수가 얇으니 한 판이 아니라 **여러 판의 폭**을 낸다
- [ ] **부푼 정도를 함께 낸다** — 맞힌 질의마다 정답을 준 갤러리 조각과의
      코사인. 높으면 연사가 떠받친 것이다. **우리 자료에서 먼저 검증할 수 있다**
- [ ] **검출기(`detect-v2-det`)를 구하면 검출까지 잰다.** 지금은 남의 몸통
      폴리곤이 그 자리를 대신하고 있다 — 그래서 **검출은 안 잰 것이다**

## 작은 것

- [ ] **NAS 뿌리를 훑게 한다.** `ndd/labels.py` 의 기본 자리가
      `/nas/JikhanJung/…` 하나로 박혀 있어 2080ti(WSL · `/mnt/p`)에서 **붙어
      있는데 없다고 나왔다.** 형제 저장소 `finseg/nas.py` 가 후보를 훑어 이미
      푼 문제인데 베껴 올 때 그 부분을 안 가져왔다. 지금은 `NDD20_DIR` 로 넘긴다
- [ ] **`BELOW` 를 할지 말지 정한다.** 표본 40장에서 마스크가 45%에서만 나왔다
      — 수중에서는 우리 seg 가 안 선다. 살리려면 그쪽 사진으로 다시 손봐야 한다

## 나중에

- [ ] **표시 내보내기**(`CLAUDE.md` 의 2단계 — `localStorage` + JSON 떨구기).
      **받을 판정이 생기면** 짓는다. 위의 *247개를 갈른다* 가 그 첫 자리다 —
      그때가 오면 짓고, 그 전에는 안 짓는다

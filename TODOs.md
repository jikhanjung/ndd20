# TODOs

앞으로 할 일. 끝난 것은 며칠만 두고 지운다 — 기록은 `devlog/` 에 남는다.

## 다음

- [ ] **분할을 바깥 자료에서 채점한다 — 지금 가장 싸고 확실한 값.**
      `ABOVE` 의 폴리라인 **2,939개**가 정답이고, **날짜도 개체 ID 도 필요
      없다.** 형제 저장소의 `seg-v3-s` 가 독립 IoU **0.841** 인데 그것은 우리가
      만든 정답에서 잰 값이다 — **남의 정답에서 얼마인지는 아무도 모른다.**
      순서: 폴리라인 → 마스크(채우는 규칙을 정해야 한다) → 우리 모델 추론 →
      IoU → **정적 HTML 로 나쁜 것부터 본다**(왜 틀렸나는 눈으로만 안다).
      **가중치는 읽기만 한다** — 형제 저장소를 안 건드린다
- [ ] **re-ID 를 재려면 앞의 셋이 먼저다** — 밑동 두 점(우리 `pose-v1` 이 다른
      개체군·다른 카메라에서 서는지부터) · 좌우 · 조각. 그 다음에야 임베딩이다
- [ ] **`BELOW` 를 어떻게 볼지 정한다.** 라벨이 두껍지만(2,233 / 82개체)
      **뒷날 notch 가 아니라 몸통 무늬**라 우리 문제가 아니다. 그래서 오히려
      *"깊이가 지렛대"*·*"계보가 갈려야 앙상블이 붙는다"* 가 **다른 종류의
      문제에도 건너가나**를 묻는 자리로는 낫다. 할지 말지를 먼저 정할 것
- [ ] **부푼 정도를 재는 자** — 맞힌 질의마다 정답을 준 갤러리 조각의 코사인을
      함께 낸다. 그 값이 높으면 연사가 떠받친 것이다. **우리 자료에서 먼저
      검증할 수 있다** — 날 건너뜀만 골라 잰 것과 비교하면 문턱이 실제로 연사를
      잡는지 확인된다

## 나중에

- [ ] **표시 내보내기**(`CLAUDE.md` 의 2단계). **받을 판정이 생기면** 짓는다 —
      미리 짓지 않는다

# TODOs

앞으로 할 일. 끝난 것은 며칠만 두고 지운다 — 기록은 `devlog/` 에 남는다.

## 다음

- [ ] **GPU 머신에서 `fins` 를 다시 돌린다.** `--device auto` 라 그대로 돈다.
      이번 판은 **마스크 모양(`poly`)까지 적는다** — CPU 판에는 바깥틀만 있어
      눈으로 볼 차례에서 왜 틀렸는지 못 가렸다. CPU 48분과 견줄 것
- [ ] **나쁜 것부터 눈으로 본다** — 담김<0.9 이거나 넓이비 1.0 초과인 914개
      (마스크의 32%). 뷰어에 결과를 얹는다. **성적은 화면이 아니라 파일이다**
- [ ] **밑동 31%를 살린다** — 마스크 2,881 중 밑동까지 간 것이 2,186 뿐이다.
      개체 ID 붙은 424 에서도 292(69%)만 간다. **조각 수를 가장 많이 늘리는
      자리다.** 왜 못 찾는지부터 눈으로 볼 것
- [ ] **re-ID 를 잰다 — 34개체 · 조각 217.** 저쪽 분류기는 **못 쓴다**(저쪽
      개체에 맞춰 녹인 것이고 NDD20 개체는 거기 없다). **임베딩만 꺼내 갤러리
      최근접**으로 잰다. 날짜가 없으니 무작위로 가르고 **그 수를 상한이라고
      명령이 찍는다.** 수가 얇으니 한 판이 아니라 **여러 판의 폭**을 낸다
- [ ] **부푼 정도를 함께 낸다** — 맞힌 질의마다 정답을 준 갤러리 조각과의
      코사인. 높으면 연사가 떠받친 것이다. **우리 자료에서 먼저 검증할 수 있다**
- [ ] **검출기(`detect-v2-det`)를 구하면 검출까지 잰다.** 지금은 남의 몸통
      폴리곤이 그 자리를 대신하고 있다 — 그래서 **검출은 안 잰 것이다**

## 나중에

- [ ] **표시 내보내기**(`CLAUDE.md` 의 2단계 — `localStorage` + JSON 떨구기).
      **받을 판정이 생기면** 짓는다 — 미리 짓지 않는다. 화면은 이미 섰지만
      **아직 아무것도 안 받는다**, 그 선은 일부러 그은 것이다

# Handoff — 현재 진행 상황

**2026-09-07. 우리 모델을 남의 사진에 돌렸다. **선다.**
다만 잰 것은 성적이 아니라 작동 여부다.**

형제 저장소(`dolfinserver2`)가 1차 바퀴를 닫은 자리에서
(개체 67 · 조각 1,300 · 3자 앙상블 top-1 89.9), **그 방법이 남의 자료에서도
서는지**를 물으러 왔다. 우리가 만들지 않은 정답으로 재는 것은 이것이 처음이다.

## 지금 있는 것

- `main.py look` — 표본을 세는 명령. `ndd/labels.py` 가 라벨을 읽는 자리 하나
- **보는 화면** — `~/venv/ndd20/bin/python manage.py runserver 0.0.0.0:8901`
  - `/` 격자. **한 칸이 사진이 아니라 지느러미 하나**다 (윤곽 바깥틀 + 여백 15%)
  - `/ind/<개체>` 한 개체만 모아 본다 — **연사를 사람 눈으로 처음 보는 자리**
  - 거르개: 갈래 · 개체 ID 있나 없나 · **안 닫힌 것(틈>0.1)** · 종
  - **DB 도 로그인도 세션도 안 켰다.** 담을 판정이 아직 없다
- **`~/venv/ndd20`** — 제 venv 다 (python 3.12 · django 5.2 · pillow 12.3).
  `requirements.txt` 가 정본이고, **명령도 화면도 같은 것으로 돈다.**
  형제 저장소의 venv 를 빌려 쓰던 것은 끊었다 — 읽기만 해도 저쪽이 판올림하면
  이쪽이 조용히 딸려 가기 때문이다
- `docs/NDD20_자료의_생김새.md` — **세어서 확인한 것만** 적은 자리
- 자료는 NAS 에 있다 (2024-05 에 받아 둔 것 · 2.5GB). `NDD20_DIR` 로 가리킨다.
  잘라 낸 미리보기는 `out/thumbs/` 에 쌓이고 저장소에 안 담는다

## 센 것 (2026-09-07)

| | 사진 | 영역 | 개체 ID | 개체 | 개체당(중앙값) |
|---|---:|---:|---:|---:|---:|
| **ABOVE** (수면 위 · 등지느러미) | 2,201 | 2,939 | **424 (14%)** | 44 | 9 |
| **BELOW** (수중 · 몸통) | 2,201 | 3,163 | 2,233 (71%) | 82 | 21 |

**ABOVE 가 우리 문제와 같은 것인데 개체 ID 가 얇다.** 우리 1차가 개체 67 ·
조각 1,300 이었으니 **3분의 1 규모**다. 종 라벨은 두껍다(BND 2,668 · WBD 271).

## 자료를 열어 보고 정한 규칙 둘

`docs/NDD20_자료의_생김새.md` 가 정본이고, 요약하면:

- **폴리라인은 사실상 닫혀 있다**(간격/크기 중앙값 0.009) — 그대로 채우면 된다.
  다만 **3%(102개)는 간격이 10%를 넘어**, 채우면 사람이 안 그린 직선이 마스크에
  들어간다. **표를 달고 성적에서 그 줄을 가른다.** 화면이 이미 그 표를 단다
- **끝점 둘은 밑동이 아니다**(가로 벌어짐 중앙값 0.01 — 한 점에서 한 바퀴 돈
  것이다). **밑동은 따로 찾아야 한다** — 형제 저장소의 `baseline.py` 를 베껴 온다

## 가장 큰 것 — **날짜가 없다**

`CLAUDE.md` 의 그 절이 정본이다. 요약하면 — 형제 저장소가 성적에 붙이는 첫
규칙(관찰일로 가르기)을 여기서는 못 쓰고, **세션을 짐작해 만들지 않기로 했다.**
무작위로 가르되 그 수를 **상한**이라고 명령이 찍게 하고, 부푼 정도는 **닮은
정도로** 따로 잰다.

## 판이 한 번 뒤집혔다 — **폴리라인은 지느러미가 아니다**

`object` 칸이 **2,939개 전부 `dolphin`** 이다. 물 위에 나온 몸통 전체이고
등지느러미는 그 일부다. 주둥이만 그린 것도 있다.

**그래서 분할 채점은 죽었다.** 우리 마스크와 IoU 를 내면 그 수는 모델 성적이
아니라 **라벨 규약 차이**다. 대신 남의 폴리곤을 **울타리**로 쓴다 — 우리 마스크가
그 안에 드는지만 본다(**담김**). 규약도 개체 ID 도 필요 없어 2,939 전부에 묻는다.

**분할은 채점 대상에서 파이프라인으로 내려갔고, 채점은 re-ID 만 남았다.**

## 잰 것 — `ABOVE` 2,939 전부 (CPU · 48분 · `out/fins/ABOVE-pad1.0.jsonl`)

| | |
|---|---:|
| 마스크를 낸 것 | **2,881 / 2,939 (98%)** |
| 밑동 두 점까지 | 2,186 (마스크의 76%) |
| 담김 중앙값 | 0.976 |
| 담김 0.5 미만 | **14** (0.5%) · 폴리곤 탓은 0 |

**`seg-v3-s` 와 `pose-v1` 이 다른 개체군·다른 카메라에서 선다.** 검출기
(`detect-v2-det`)는 이 기계에 없어 **남의 몸통 폴리곤 바깥틀을 상자 자리에**
썼다. `--pad` 는 물려받지 않고 재서 1.0으로 골랐다(저쪽 2.0은 지느러미 상자용).

### re-ID 로 갈 수 있는 자리

| | |
|---|---:|
| 개체 ID 붙은 영역 | 424 → 마스크 **424 (100%)** → 밑동 292 (69%) |
| 깨끗한 조각 후보 | **224** (담김≥0.9 · 넓이비 정상 · 폴리곤 닫힘) |
| 2장 이상인 개체 | **34개체 · 조각 217** |

찍기가 2.9%다. 저쪽 1차(개체 67 · 조각 1,300)의 6분의 1이라 **소수점은 못
믿지만 70%냐 15%냐는 갈린다.** 밑동에서 31%가 빠지는 것이 가장 큰 손실이다.

## BELOW 는 안 선다 (표본 40)

마스크가 45%에서만 나오고, 그중 5개는 담김 0.5 미만이다. **폴리곤 탓이
아니다** — 틀린 것들의 틈은 0.005 아래인데 마스크 넓이가 크롭의 28~43%다.
지느러미가 아니라 몸통이나 물을 통째로 두른 것이다. 수중을 살리려면 seg 를
그쪽 사진으로 다시 손봐야 한다.

> 수중이면 밑동이 수면에 안 잘려 **실제로 보인다**는 이점이 있다. 다만
> `pose-v1` 이 배운 밑동은 **수면선**이라 그 두 점은 다른 것을 가리킨다.
> BELOW 안에서만 재면 일관되지만 **ABOVE 조각과 섞으면 안 된다.**

## 다음

1. **GPU 머신에서 다시 돌린다** — `--device auto` 다. `README.md` 에 필요한
   것을 적어 뒀다. CPU 48분과 견줄 것
2. **눈으로 본다** — 볼 것이 914개(마스크의 32%)다. 이번 판부터 **마스크 모양을
   적는다**(`poly`). 바깥틀만 있으면 왜 틀렸는지 못 가린다
3. **밑동 31%를 살린다** — 여기가 조각 수를 가장 많이 늘린다
4. 그 다음에 **re-ID 34개체 · 217조각**
